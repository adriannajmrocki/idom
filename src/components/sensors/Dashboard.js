import React, { Fragment } from 'react';
import SensorsList from './SensorsList';

const Dashboard = () => {
  return (
    <Fragment>
      <SensorsList />
    </Fragment>
  )
}

export default Dashboard;